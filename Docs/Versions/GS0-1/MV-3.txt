Changed automatic named session name
--> len(Session) to a new variable in internalData 'SessionName'
    --> Each time a new session is created, SessionName is added by 1
    --> If a session is deleted, two session cannot have the same name now

Added an Error in GlobalE 
--> 'BadUtilisation' for user error in the management of the session or bad utilisation of command
    --> Created to be used in Webartifact.py only
    --> Added a raise with it for the same utilisation of a session name 
    --> Very simple error class with not a lot of parametre
    
Correction of the already used port in the module
--> the if statement compared an int Port with a list of str Port and the error never occured (GlobalFunction.VerifyUserSettings)

Test new gitignore file

Commit : {0.1.3} [B-Fixe A-Error]