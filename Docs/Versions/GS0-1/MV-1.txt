Detailled already present raise error with context and variable
Base Port settings is now set on "auto"
--> If "auto" is selected, a random registred port will be choosed
    --> New function GetFreeRegistredPort
        --> Try to find a port in list range ((4434, 4440), (4461, 4479), (4489, 4499), (4504, 4533)) (not official registred port) and if no ports is found, a random no used registred port is choosed
Modified .gitignore
--> deleted all the content and added .log / pycache and l.py 
--> deleted these file on the repositorie

Modified Docs files
--> Deleted SimpleDocumentation.md and HardDocumentation.md to create one file for all : Documentation.md
--> Added categorys / sub categorys / ... and started filling the content

Commit : {0.1.1} : [A-Docs] [A-Function]